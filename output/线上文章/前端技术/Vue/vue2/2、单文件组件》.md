---
title: ".2、单文件组件》"
slug: "vue-vue2-2-7a115d2f"
summary: ""
category: "vue2"
tags: []
status: "draft"
sortOrder: 90
cover: ""
originalId: "6a2d291e8a2b1c68f2cac2c4"
originalSlug: "vue-vue2-2-7a115d2f"
originalStatus: "published"
exportedAt: "2026-07-30T14:30:35.933Z"
---
# 3.2、单文件组件
## 3.2.1. 什么是单文件组件？
1. **一个文件对应一个组件**  
（之前我们所学的是非单文件组件，一个HTML文件中定义了多个组件）
2. **文件扩展名**  
单文件组件的名字通常是：`x.vue`，这是Vue框架规定的。只有Vue框架能够识别，浏览器无法直接打开运行。需要Vue框架进行编译，将`x.vue`最终编译为浏览器能识别的HTML、JS、CSS。
3. **文件名命名规范**  
单文件组件的文件名命名规范和组件名的命名规范相同：
    - 全部小写：`userlist`  
    - 首字母大写，后面全部小写：`Userlist`  
    - kebab-case命名法：`user-list`  
    - CamelCase命名法：`UserList`（推荐使用这种方式，与Vue开发者工具呼应）  
注意：不能用小驼峰：`userList`；不能用HTML标签名。

## 3.2.2. `x.vue`文件的内容包括三块：
1. **结构**  
`<template>HTML代码</template>`  
2. **交互**  
`<script>JS代码</script>`  
3. **样式**  
`<style>CSS代码</style>`

## 3.2.3. `export`和`import`，ES6的模块化语法
1. **使用**`export`**导出（暴露）组件**  
在需要使用组件的`x.vue`文件中使用`import`导入组件：
    - **默认导入和导出**  

```javascript
export default {}
import 任意名称 from '模块标识符'
```

    - **按需导入和导出**  

```javascript
export {a, b}
import {a, b} from '模块标识符'
```

    - **分别导出**  

```javascript
export var name = 'zhangsan'
export function sayHi(){}
```

**导入**  

```javascript
import {name, sayHi} from '模块标识符'
```

## 3.2.4. VSCode插件
VSCode工具可以安装一些插件，这样在编写`x.vue`时会有提示。例如：`vetur`插件。

+ 使用该插件后，有高亮显示，并且可以通过输入辅助编写代码。

## 3.2.5. 第一个单文件组件
将之前“组件嵌套”的例子修改为单文件组件。

### 要领
不管是单文件组件还是非单文件组件，永远都包括三步：

1. 创建组件  
2. 注册组件  
3. 使用组件

创建Vue实例的代码不是一个组件，通常写在一个单独的JS文件中，一般命名为`main.js`（寓意：入口）。

还剩最后一点HTML代码，通常这个文件叫做`index.html`，代码如下：

### 代码执行原理
1. **第一步**：浏览器打开`index.html`页面，加载容器。  
2. **第二步**：加载`vue.js`文件，引入Vue框架。  
3. **第三步**：加载`main.js`。  

```javascript
import App from './App.vue'
import son from './son.vue'
import gril from './gril.vue'
import child from './child.vue'
```

这样就完成了所有组件以及子组件的创建和注册。  

4. **第四步**：创建Vue实例`vm`，编译模板语句，渲染。

**注意**：写完代码后不能直接运行，浏览器不认识`.vue`文件，也不认识ES6的模块化语法。需要安装Vue脚手架。

---
