---
title: "第 23 章：常见问题与面试高频点"
slug: "vue-ai-vue-b084ddd6"
summary: "Vue 常见问题与面试高频点，适合作为课程学习后的复习和查漏补缺资料。"
category: "Ai的vue"
categoryPath:
  - "前端技术"
  - "Vue"
  - "Ai的vue"
tags: []
status: "published"
sortOrder: 230
cover: ""
originalId: "6a2d291e8a2b1c68f2cac27c"
originalSlug: "vue-ai-vue-b084ddd6"
originalStatus: "published"
publishedAt: "2026-02-02T13:17:53.475Z"
updatedAt: "2026-07-31T11:16:23.860Z"
exportedAt: "2026-08-03T03:03:53.296Z"
---
# 第 23 章：常见问题与面试高频点

> 目标：把 Vue2 的高频“为什么”问题系统梳理，既能写代码，也能讲清原理。

---

## 23.1 `data` 为什么必须是函数

结论：

- 在组件里 `data` 必须返回新对象，保证每个组件实例有独立数据。

如果 `data` 是对象：

- 多个实例会共享同一份引用
- A 改了数据，B 也变了

---

## 23.2 computed vs watch 区别

- computed：
  - 算出一个值（派生状态）
  - 有缓存
  - 适合纯计算

- watch：
  - 监听变化做事（副作用/异步）
  - 适合请求、写缓存、防抖等

---

## 23.3 `nextTick` 的使用场景与原理（概念级）

使用场景：

- 数据变更后要操作更新后的 DOM

原因：

- Vue 的 DOM 更新是异步批处理的

典型：

- 打开弹窗后立刻 focus 输入框
- 列表渲染后计算高度

---

## 23.4 Vue2 响应式缺陷总结

Vue2 基于 `Object.defineProperty`，主要缺陷：

- 新增对象字段默认不响应
- 数组按索引赋值/修改 length 默认不响应

解决：

- `Vue.set / this.$set`
- 初始化时声明完整字段

---

## 23.5 虚拟 DOM 与 diff 思想（概念级）

虚拟 DOM：

- 用 JS 对象描述真实 DOM
- 数据变化后生成新的 VDOM

diff：

- 对比新旧 VDOM，找出最小变更
- 再把变更应用到真实 DOM

`key` 的意义：

- 帮助 diff 更准确识别列表项身份

---

## 23.6 组件通信方案对比与选型

常用选型（推荐顺序）：

- 父子：props / $emit
- 兄弟：父组件中转
- 跨层共享配置：provide/inject
- 少量跨组件事件：EventBus（小项目）
- 全局共享状态：Vuex

---

## 本章小结

- 会写不够，面试更关注“为什么”。
- data/computed/watch/nextTick/响应式缺陷/key/diff 是 Vue2 高频必问。
- 组件通信优先保持数据流清晰，规模上来用 Vuex。

**下一章预告**

第24章进入 Vue3 过渡：Proxy 响应式、Composition API、Pinia、Vite，以及 Vue2 → Vue3 的迁移差异清单。
