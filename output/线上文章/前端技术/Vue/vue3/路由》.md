---
title: "路由》"
slug: "vue-vue3-21013aa4"
summary: ""
category: "vue3"
tags: []
status: "draft"
sortOrder: 20
cover: ""
originalId: "6a2d291f8a2b1c68f2cac2d6"
originalSlug: "vue-vue3-21013aa4"
originalStatus: "published"
exportedAt: "2026-07-30T14:08:39.359Z"
---
## 5.1、 路由的基本使用
点击对应tab，跳转到对应的页面

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2024/png/27167233/1734612084083-43e5ebf0-158d-4667-aa50-fba3eedf48c4.png)

### 5.3.1、安装路由vue-router
 vu3要安装vue-router4，参考官网：[https://router.vuejs.org/zh/](https://router.vuejs.org/zh/)

```vue
npm i vue-router@4
```

如果报错

<!-- 这是一张图片，ocr 内容为：(BASE) WANGCHUNYAN@WANGCHUYANDEMBP SRC % NPM INSTAL INSTALL VUE-ROUTER ERR! CODE ERESOLVE NPM ERESOLVE UNABLE TO RESOLVE DEPENDENCY TREE NPM ERR! NPM WHILE RESOLVING:VUE_CLI_PRO@0.1.0 ERR! NPM FOUND:VUE@2.7.16 ERR! NPM ERR! NODE MODULES/VUE NPM ERR! VUE@"2.6.11"FROM THE ROOT PROJECT NPM ERR! NPM COULD NOT RESOLVE DEPENDENCY: ERR! NPM "FROMVUE-ROUTER@4.3.0 ERR! PEER VUE@".0"FR NPM ERR! NODE MODULES/VUE-ROUTER NPM VUE-ROUTER@"*" FROM THE ROOT PROJECT ERR! NPM ERR! NPM ERR! FIX THE UPSTREAM DEPENDENCY CONFLICT, OR RETRY NPM TH--FORCE OR --LEGACY-PEER-DEPS ERR!THIS COMMAND WITH --F NPM -->
![](https://cdn.nlark.com/yuque/0/2024/png/27167233/1726123354477-ea737a01-c1e4-4996-82f5-663a7ea13fd7.png)

npm i [vue-router@3](http://vue-router@3) --legacy-peer-deps

### 5.3.2、配置vue-router
src/router/index.ts	配置路由器对象，并暴露

```javascript
import {createRouter,createWebHistory} from 'vue-router'
// 导入路由组件
import Index from '../views/index.vue'
import News from '../views/news.vue'
import Car from '../views/car.vue'
import Mine from '../views/mine.vue'
// 配置路由路径
const routes=[
    {
        path:"/index",
        component:Index
    },
    {
        path:"/news",
        component:News
    },
    {
        path:"/car",
        component:Car
    },
    {
        path:"/mine",
        component:Mine
    }
]
// 创建路由
const router =createRouter({
    // 不仅要有路由规则，还要设置监听URL的模式
    // 用于配置路由模式的选项。
    // 它指定了路由应该使用 HTML5 的历史记录 API (window.history) 来实现导航
    history:createWebHistory(),
    routes
})
// 对外暴露
export default router
```

**main.ts**

```javascript
import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

// 导入路由配置
import router from './router/index'

const app=createApp(App)

// 注册路由
app.use(router)

app.mount('#app')

```

**app.vue**

```vue
<script setup>
import navBar from './components/navBar.vue';
import { RouterLink, RouterView } from 'vue-router';
</script>

<template>
  <div class="app">
    <!-- 头部导航 -->
    <navBar class="navbar" />
    <div class="main">
      <!-- 路由容器 -->
      <RouterView></RouterView>
    </div>
    <!-- 底部导航 -->
    <div class="footer">
      <RouterLink to="/index" active-class="active">首页</RouterLink>
      <RouterLink to="/news" active-class="active">新闻</RouterLink>
      <RouterLink to="/car" active-class="active">购物车</RouterLink>
      <RouterLink to="/mine" active-class="active">我的</RouterLink>
    </div>
  </div>
</template>
<style>
.app {
  width: 375px;
  height: 500px;
  display: flex;
  flex-direction: column;
  line-height: 50px;
}

.navbar {
  width: 100%;
  height: 50px;
  background-color: #ccc;
  display: flex;
  justify-content: space-between;
}

.main {
  flex: 1;
  background-color: pink;
}

.footer {
  height: 50px;
  line-height: 50px;
  background-color: #ccc;
  display: flex;
  justify-content: space-around;
}
</style>
```

注意事项：

① 路由组件一般会和普通组件分开存放，路由组件放到**pages**目录，普通组件放到components目录下。

②<u> 路由组件在进行切换的时候，切掉的组件会被销毁，</u>可用destroyed生命周期钩子证实

③ 路由组件的两个属性：<font style="color:#DF2A3F;">$route和$router</font>

1) $route：属于自己的路由对象。

2) $router：多组件共享的路由器对象。

## 5.2、 路由重定向&路由命名
### （1）、路由重定向
1. <font style="color:rgb(51, 51, 51);">作用：</font><u><font style="color:rgb(51, 51, 51);">将特定的路径，重新定向到已有路由。</font></u>
2. <font style="color:rgb(51, 51, 51);">具体编码</font>

router/index.ts

```javascript
const routes = [
    // 1、路由重定向
    {
        path: '/',//表示初始页面
        // redirect:'/index'//重定向到index组件
        redirect: { name: 'index' }
    },
    // 2、404页面,优先级最低，保底的，前面路由都没匹配上，就会走他
  // 相比vue2的 * ，vue3需要写成正则形式
    {
        path:"/:pathMatch(.*)*",
        component:NotFound
    }
  //·······
]
```

#### <font style="color:rgb(6, 6, 7);">为什么使用 </font>`/:pathMatch(.*)*`<font style="color:rgb(6, 6, 7);"> 而不是直接使用 </font>`*`<font style="color:rgb(6, 6, 7);">？</font>
1. **<font style="color:rgb(6, 6, 7);">匹配所有路径</font>**<font style="color:rgb(6, 6, 7);">：</font>`:pathMatch(.*)*`<font style="color:rgb(6, 6, 7);"> 是一个动态参数，结合了正则表达式 </font>`(.*)*`<font style="color:rgb(6, 6, 7);">，表示匹配任意字符（包括空字符），并且可以匹配多次。这确保了无论用户访问的是简单路径还是嵌套路径，都能被捕获</font><font style="color:rgb(6, 6, 7);">。</font>
2. **<font style="color:rgb(6, 6, 7);">捕获完整路径信息</font>**<font style="color:rgb(6, 6, 7);">：通过这种方式，匹配到的路径会被作为参数传递给组件。在组件中可以通过 </font>`this.$route.params.pathMatch`<font style="color:rgb(6, 6, 7);"> 获取完整的路径信息。这对于实现动态的 404 页面或日志记录等功能非常有用</font><font style="color:rgb(6, 6, 7);">。</font>
3. **<font style="color:rgb(6, 6, 7);">避免与其他路由冲突</font>**<font style="color:rgb(6, 6, 7);">：如果直接使用 </font>`*`<font style="color:rgb(6, 6, 7);">，可能会与其他路由冲突。而 </font>`/:pathMatch(.*)*`<font style="color:rgb(6, 6, 7);"> 是一个明确的通配符路由，应该始终放在所有其他路由的最后，以确保其他路由能够正确匹配。</font>

### （2）、路由命名
<font style="color:rgb(51, 51, 51);">作用：可以简化路由跳转及传参（后面就讲）。</font>

<font style="color:rgb(51, 51, 51);">给路由规则命名：</font>

```javascript
const routes = [
    {
        path: "/index",
        name: 'index',
        component: Index
    },
    {
        path: "/news",
        name: 'news',
        component: News
    },
    {
        path: "/car",
        name: 'car',
        component: Car
    },
    {
        path: "/mine",
        name: 'mine',
        component: Mine
    }
]
```

怎么使用？必须使用 **<font style="color:rgb(255, 0, 0);">:to=”{}”</font>** 的方式

```javascript
  <!-- <RouterLink to="/mine" active-class="active">我的</RouterLink> -->
<RouterLink :to="{name:'mine'}" active-class="active">我的</RouterLink>
```

## 5.3 、嵌套路由
<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2024/png/27167233/1734619452304-2bb82b85-dd2d-4a0f-9472-6326914fb14e.png)

准备好index的子路由

配置路由规则，使用children配置项

router/index.ts

```javascript
//····
{
        path: "/index",
        name: 'index',
        component: Index,
        children: [
            {
                path: 'like',//写法一，不加/
                name:'like',
                component: like
            },
            {
                path: '/index/wait',//写法二，完整路径
                name:'wait',
                component: wait
            },
        ]
    },
//·····
```

index.vue

注意：

<font style="color:rgb(51, 51, 51);">跳转路由（</font><font style="color:#DF2A3F;">记得要加完整路径</font><font style="color:rgb(51, 51, 51);">）</font>

<font style="color:rgb(51, 51, 51);">预留一个`<router-view>`</font>

```javascript
<script setup>
</script>
<template>
    <div>
        <div class="swiper">轮播图</div>
        <nav>
            <!-- 点击跳转路由 -->
            <RouterLink active-class="active" to="/index/like">猜你喜欢</RouterLink>
            <RouterLink active-class="active" :to="{ name: 'wait' }">最受期待</RouterLink>
        </nav>
        <!-- 二级路由容器 -->
        <router-view></router-view>
    </div>
</template>
<style scoped>
.swiper {
    width: 100%;
    height: 120px;
    background-color: #bfa;
}

nav {
    height: 40px;
    line-height: 40px;
    border-bottom: 1px solid red;
    display: flex;
    justify-content: space-around;
}
</style>
```

## **5.4 、声明式路由传参	**
**需求：**点击电影，请求不同的数据，显示在detail组件上

为了提高组件的复用性，可以给路由组件传参

传递参数：哪里点击跳转，在哪里传参数

### 5.5.1、路由中引入detail组件，并配置路由
**router/index.js**

```javascript
//.....
import Detail from '../views/detail.vue'
const routes = [
    {
        path: '/detail',
        component: Detail
    },
  //.......
```

### 5.2.2、在哪里跳转就在哪里传参数
**like.vue**

```javascript
<script setup>
import { onMounted, reactive, ref } from 'vue';
import axios from 'axios'
const list = ref([])
onMounted(async () => {
    // 请求卖座网数据，注意加请求头
    let res = await axios({
        url: `https://m.maizuo.com/gateway?cityId=310100&pageNum=1&pageSize=10&type=1&k=9724615`,
        headers: {
            "x-client-info": ' { "a": "3000", "ch": "1002", "v": "5.2.1", "e": "173211553215306425924321281", "bc": "310100" }',
            "x-host": "mall.film-ticket.film.list"
        }
    })
    // console.log(list);

    list.value = res.data.data.films
    // console.log(list);
    // console.log(res.data.data.films);
})
</script>
<template>
    <div>
        <ul class="like">


            <li v-for="item in list" :key="item.id">
                <!-- 第一种传参方式：固定值   /路径？参数1名=参数值 & 参数2名=参数值 -->
                <!-- <RouterLink to="/detail?a1=1&a2=2&a3=3"> -->
                <!-- 第二种传参方式：动态读取数据 -->
                <!-- <RouterLink :to="`/detail?id=${item.filmId}`"> -->
                <!--第三种传参方式：query对象传参数 ,可以用path或者name都可以-->
                <!-- <RouterLink :to="{
                    // path: '/detail',
                    name: 'detail',
                    query: {
                        id: item.filmId
                    }
                }"> -->
                <!--第四种传参方式：params对象传参数 ,只能用name，且需要在配置路由时占位-->
                <RouterLink :to="{
                    name: 'detail',
                  // 这个属性名id需要跟占位符名字一致
                    params: {
                        id: item.filmId
                    }
                }">
                    {{ item.name }}
                </RouterLink>
            </li>
        </ul>
    </div>
</template>
<style scoped>
.like {
    overflow: auto;
    height: 400px;
}
</style>
```

```javascript
<!-- 1、传递静态数据 -->
        <!-- <router-link to="/detail/123/456" class="film"> -->
        
        <!-- 2、传递动态的数据 -->
        <!-- <router-link :to="`/detail/${film.filmId}/456`" class="film"> -->
        
        <!-- 3、传递参数：对象的形式，不能用path，必须用name -->
        <!-- :to="{
          // path: '/detail',
          name: 'detail',
          params: {
            filmId: film.filmId,
            num2: 456
          }

        }" -->
```

**router/index.js **

<font style="color:#DF2A3F;">Vue3的params传参，无论哪种传参都需要占位</font>，此时的id是参数的名子，传递过去的是参数的值

注意：Vue2的对象传参可以不占位

```javascript
{
        path: '/detail/:id',
        name: 'detail',
        component: Detail
    },
```

### 5.5.3、接收参数
<u>接收参数，在路由组件中接收参数</u>

**detail.vue**

```javascript
<script setup>
    // 需要先导入
    import { useRoute } from 'vue-router';
    // 创建当前路由实例
    const route = useRoute()

    // query传参接收方式
    // let id = route.query.id
    // params传参接收方式
    let id = route.params.id
    console.log(id);
</script>
```

## **5.5 、****<font style="color:rgb(0, 0, 0);">路由的props	</font>**
props配置主要是为了<u>简化query和params参数的接收。</u>让插值语法更加简洁。

三种实现方式

```javascript
 {
        path: '/detail/:id',
        name: 'detail',
        component: Detail,
        //第一种写法，自己决定传递什么参数，固定值，用的比较少
        // props: {
        //     a: '1111',
        //     b: '2222',
        //     c: '3333'
        // }

        //第二种写法： props的布尔值写法，作用：只针对params传参
        // props: true
          
        // 第三种写法：函数写法，作用：把返回的对象中每一组key-value作为props传给Detail组件
        props(route) {
            return { id: route.params.id, title: '详情' }
        }

    },
```

**detail.vue props接收，直接可以使用**

```javascript
<script setup>
    import { defineProps } from 'vue';
    import { useRoute } from 'vue-router';
    const route = useRoute()
    // 这种获取方式比较麻烦，可以通过props去简化参数的接收
    // let id = route.query.id
    // let id = route.params.id
    // console.log(id);

    // props接收数据
    // const props=defineProps(['a','b','c'])
    // const props=defineProps(['id'])//每一组params参数，都自动传给detail组件
    const props=defineProps(['id','title'])
    console.log(props);//里面封装着router.js传递的数据
</script>
```

## 5.6、 编程式路由导航	
通过点击超链接的方式切换路由，这种方式，我们叫**声明式的路由导航**

通过编写代码，完成路由组件的切换，这种方式我们叫**编程式路由导航**

声明式路由导航可以通过相关API来完成：

setup语法糖中，路由组件的两个重要的属性：`$route`和`$router`变成了两个`hooks`

```javascript
//导入模块
import {useRoute,useRouter} from 'vue-router'
// 创建route  router
const route = useRoute() //获取的是路由对象
const router = useRouter() //取的是整个项目的路由器对象（一般一个项目就一个路由器对象）
// 组件上拿参数
console.log(route.query)
console.log(route.parmas)
// 点击跳转
console.log(router.push)
console.log(router.replace)
```

(1) push模式：

:::info
router.push({})				有历史记录

:::

(2) replace模式：

:::info
 router.replace({})			无历史记录

:::

(3) 前进：

:::info
 router.forward()

:::

(4) 后退：

:::info
 router.back()

:::

(5) 前进或后退几步：

:::info
router.go(2) 前进两步

 router.go(-2) 后退两步

:::

```javascript
<script setup>
//·······
import { useRoute, useRouter } from 'vue-router';
const route = useRoute()
const router = useRouter()
const gotoDetail = (filmId) => {
    // 1、params 拼接传参，注意定义路由时要占位
    // router.push(`/detail/${filmId}`)
    // 2、params 对象传参，此时注意只能使用name
    // router.push({
    //     name: 'detail',
    //     params: {
    //         id: filmId
    //     }
    // })
    // 3、query 拼接传参
    // router.push(`/detail?id=${filmId}&title='haha'`)
    // 4、query对象传参
    router.push({
        path: '/detail',
        // name:'detail',
        query: {
            id: filmId
        }
    })
}
//·······
 const back = () => {
    router.back()
}

const back2 = () => {
    router.go(-2)
}

const go2 = () => {
    router.go(2)
}

const foward = () => {
    router.forward()
}
//······
</script>
```

## 5.7、路由器工作模式
1. `history`模式

> 优点：`URL`更加美观，不带有`#`，更接近传统的网站`URL`。
>
> 缺点：后期项目上线，需要服务端配合处理路径问题，否则刷新会有`404`错误。
>

```javascript
const router = createRouter({
    history:createWebHistory(), //history模式
    /******/
})
```

2. `hash`模式

> 优点：兼容性更好，因为不需要服务器端处理路径。
>
> 缺点：`URL`带有`#`不太美观，且在`SEO`优化方面相对较差。
>

```javascript
const router = createRouter({
    history:createWebHashHistory(), //hash模式
    /******/
})
```

## 5.8、路由懒加载
当打包构建应用时JavaScript包会变得非常大，影响页面加载。如果我们能把不同路由对应的组件分割成不同的

代码块，然后当路由被访问的时候才加载对应组件，这样就会更加高效。

Vue Router支持开箱即用的动态导入，这意味着你可以用动态导入代替静态导入：

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2024/png/27167233/1734694917832-404527c6-cfc6-4eac-8628-114cc189947f.png)

```javascript
//将
//import userDetails from './views/userDetails.vue'
//替换成
const userDetails => import（'./views/userDetails.vue'）

const router =createrouter({
  routes:[
    {
     path: '/users/:id',
     component:userDetails 
    }
  ],
})

```

## 5.9、 路由守卫	
不同的守卫其实就是在不同的时机，不同的位置，添加鉴权（鉴别权限）代码

**需求：**并不是所有页面都可以随便访问的，如果想看购物车页面，需要先登录

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2024/png/27167233/1734686129635-14e63cef-2fc7-421c-b394-f0940d9787d6.png)

### 5.9.1 全局前置守卫 	
1、书写位置：在创建好router之后，以及暴露router之前

2、执行时机：beforeEach()中传入一个回调函数callback，可以是普通函数或箭头函数都可以，在初始化时执行一次，然后每一次在切换任意组件之前都会被调用

:::info
router.beforeEach((to, from, next)=>{ // 翻译为：每次前（寓意：每一次切换任意路由之前执行。）

// to 去哪里(**<font style="color:rgb(255, 0, 0);">to.path、to.name</font>**)

// from 从哪来

// next 继续：调用next( )

})

:::

3、callback函数有3个参数：to form next

from参数：from是一个路由对象，表示从哪里来（从那个路由切过来的），起点

to参数：to也是一个路由对象，表示到哪里去，终点

next参数：是一个函数，调用这个函数之后，表示放行，可以继续向下走

4、给路由对象添加自定义属性的话，需要在路由对象的meta（路由元）中定义

如果路由组件较多。to.path会比较繁琐，可以考虑给需要鉴权的路由扩展一个布尔值属性，可以通过路由元来定义属性：**<font style="color:rgb(255, 0, 0);">meta:{isAuth : true}</font>**

```javascript
const router = createRouter({
    history: createWebHistory(),
    routes
})

// 1、全局前置路由守卫
// 需求：如果要去购物车页面，判断有没有登录，如果没有就不给看
router.beforeEach((to, from, next) => {
    let token = localStorage.getItem('token')
    // 不是去购物车页面，进行放行
    // 判断方式1 判断name
    // if (to.name == 'car') {
    // 判断方式2  判断path
    // if (to.path == '/car') {
    // 判断方式3  判断meta配置
    if (to.meta.isAuth) {

        if (token) {
            next()
        } else {
            alert('请登录') //给提示，让登录
            next({ path: '/login' }) //跳转登录页面
        }

    } else {
        next()
    }
    // console.log(to);
    // next()
})

export default router
```

### 5.9.2 全局后置守卫	
**需求： 每次切换完路由组件后，更换title标题栏，在meta中设置title属性属性值**

1、书写位置：router/index.js文件中拿到router对象，在创建router对象之后，暴露router对象之前写

2、执行时机：初始化执行一次，以后每一切换完任意路由之后

3、参数：只有to,from,没有next，因为没有必要了

:::info
router.afterEach((to, from)=>{ // 翻译为：每次后（寓意：每一次切换路由后执行。）

// 没有 next

document.title = to.meta.title // 通常使用后置守卫完成路由切换时title的切换。

})

:::

4、主要可以用来用户访问的路径，用来刻画用户画像

该功能也可以使用前置守卫实现：

```javascript
//需求： 每次切换完路由组件后，更换title标题栏，在meta中设置title属性属性值
// 书写位置：在创建router对象之后，暴露router对象之前
// 执行时机：初始化执行一次，以后每一切换完任意路由之后
// 参数：只有to,from,没有next，因为没有必要了
router.afterEach((to, from) => {
  document.title = to.meta.title || "欢迎使用";
});
```

### 5.9.3 局部路由守卫之path守卫（路由守卫）	
beforeEnter(){} 

书写位置：写在route对象中

执行时机：进入到对应路由组件前被调用

参数：to,from,next

注意：没有afterEnter

```javascript
 {
        path: "/car",
        name: 'car',
        component: Car,
        meta: {
            isAuth: true,
            title: '购物车'
        },
        // 3、局部路由守卫
        beforeEnter(to, from, next) {
            let token=localStorage.getItem('token')
            if (token) {
                next()
            } else {
                alert('没有权限')
                next('/login')
            }
        },
    },
```

### 5.9.4 局部路由守卫之component守卫（组件守卫）	
书写位置：写在路由组件当中，也就是xxx.vue文件中

执行时机： beforeRouteEnter	进去路由组件之前执行

 		 beforeRouteLeave	离开路由组件之前执行 

注意：只有路由组件才有这两个钩子。

```javascript
<script>
export default {
    name: 'subject2',
    // 进去路由组件之前执行，此时组件实例还没有被创建
    beforeRouteEnter(to, from) {
        console.log(`进入路由组件之前“${to.meta.title}`)
    },
  // 在当前路由改变，但是该组件被复用时调用
    beforeRouteUpdate(to, from) {
        console.log(`进入路由组件之前“${to.meta.title}`);
    },
    // 离开路由组件之前执行
    beforeRouteLeave(to, from) {
        console.log(`离开路由组件之前“${from.meta.title}`);

    }
}
</script>
```

在setup语法糖中

```javascript
<script setup>
import { watch } from 'vue';
import { onBeforeRouteLeave, onBeforeRouteUpdate, useRoute, useRouter } from 'vue-router';
const route = useRoute()
const router = useRouter()

// 这种获取方式比较麻烦，可以通过props去简化参数的接收
let id = route.query.id

// 在当前的详情页面，跳转到另一个电影的详情页面
// 之前从like跳过来的时候，走的时onmount的函数，这个函数只会渲染一次，
// 此时再点击无法，无法再次请求函数，要么用wacth监听route.query的变化
// 要么在组件更新守卫里重新请求数据

const goDetailLike = () => {
    router.push({
        name: 'detail',
        query: {
            id: '5566'
        }
    })
}

onBeforeRouteUpdate((to, from) => {
    console.log(`根据${to.query.id}请求数据`);
})
onBeforeRouteLeave(() => {
    let flag = window.confirm('你确定要离开吗')
    if (!flag) return false
})
// 注意没有onBeforeRouteEnter，如果要用，就不能语法糖模式
</script>
<template>
    <div>
        <h1>detail组件-{{ id }}</h1>
        <button @click="router.back()">返回</button>
        <hr>
        <h2>猜你喜欢</h2>
        <ol>
            <li @click="goDetailLike">忠犬八公</li>
        </ol>
    </div>
</template>
<style scoped></style>
```
