---
title: "前端面试知识点整理"
slug: "legacy-cfbd06b8-cfbd06b8-revision-20260730"
summary: "合并整理前端面试零碎知识与补充题库，涵盖 Vue2/Vue3、Vuex/Pinia、Diff、ECharts、小程序、WebSocket、Three.js、工程化与常见基础题。"
category: "面试"
tags:
  - "Vue2"
  - "Vue3"
  - "小程序"
  - "ECharts"
  - "WebSocket"
  - "前端面试"
status: "draft"
sortOrder: 30
cover: ""
originalId: "6a2d29208a2b1c68f2cac696"
originalSlug: "legacy-cfbd06b8-cfbd06b8"
originalStatus: "published"
exportedAt: "2026-07-30T13:20:22.058Z"
---
# 前端面试知识点整理

这篇笔记是把原来的《零碎知识》和《郭齐龙面试题》合并后的版本，尽量保留两篇里的所有有效内容，只对重复部分做合并整理，方便后续统一复习。

![参考截图](https://cdn.nlark.com/yuque/0/2025/png/50923934/1750238665292-96ae4ab7-63dd-41cc-8281-6acb68d92e1d.png)

## 1. Vue2 和 Vue3 的区别

### 1.1 核心区别

- 响应式原理：Vue2 基于 `Object.defineProperty`，Vue3 基于 `Proxy`
- API 风格：Vue2 主要是选项式 API，Vue3 同时强化了组合式 API
- TypeScript 支持：Vue3 对 TS 更友好
- 性能：Vue3 在编译和运行时都做了更多优化
- 全局 API 与部分语法：Vue3 对一些旧写法做了调整或移除

### 1.2 Vue2 的缺点

1. 响应式系统基于 `Object.defineProperty`，需要递归遍历对象属性，初始化开销较大
2. 无法监听新增/删除属性，需要借助 `Vue.set`/`Vue.delete` 手动触发更新
3. 无法直接监听数组索引变化和 `length` 属性修改，需要重写数组方法
4. Options API 容易让同一块业务逻辑分散在不同选项中，代码组织不够灵活
5. 深层对象需要递归劫持，初始化性能开销较大
6. TypeScript 支持相对薄弱，类型推导能力有限

### 1.3 Vue3 的优势

1. 使用 `Proxy` 重构响应式系统，支持动态增删属性，也能监听 `Map`、`Set`
2. 数组索引变化可以被直接追踪
3. 更适合组合式封装，逻辑复用更自然
4. 更好地支持 TypeScript
5. 编译优化更多，例如静态提升、`Patch Flag`、Block Tree
6. 更容易做 Tree Shaking，减小打包体积

### 1.4 Vue2 和 Vue3 的响应式原理

- Vue2：通过 `Object.defineProperty` 劫持对象属性的 `getter` 和 `setter`
- Vue3：通过 `Proxy` 代理整个对象，能力更完整

### 1.5 数据劫持

数据劫持是 Vue 响应式的底层思想之一。Vue2 会把 `data` 中的数据通过 `Object.defineProperty` 转成带 `getter` 和 `setter` 的属性。当数据被读取或修改时，就能在中间插入依赖收集与视图更新逻辑，所以叫“劫持”。

更通用地说：

- Vue2 主要依赖 `Object.defineProperty`
- Vue3 主要依赖 `Proxy`
- 本质都是在数据变化时自动通知视图更新

## 2. 虚拟 DOM、Diff 和 MVVM

### 2.1 什么是虚拟 DOM

虚拟 DOM 是对真实 DOM 结构的 JavaScript 对象描述。框架在数据变化后，先生成新的虚拟 DOM，再对比新旧差异，最后把最小更新同步到真实 DOM。

### 2.2 Diff 算法原理

Diff 算法用于比较新旧虚拟 DOM 的差异，以更高效地更新真实 DOM。

核心思路：

- 同层比较，不做复杂的跨层级对比
- 优先借助 `key` 判断节点是否可复用
- 只更新变化的部分，不整棵树重建

### 2.3 Diff 算法特征

- Key 优先匹配
- 同层级比较
- 最小化 DOM 操作
- 通过同层比较和 key 优化，将传统树对比的 `O(n³)` 复杂度降低到 `O(n)`

### 2.4 Vue2 和 Vue3 的 Diff 差异

- Vue2 常见描述是双端比较策略
- Vue3 对动态子节点处理做了进一步优化
- Vue3 会结合最长递增子序列（LIS）减少无意义移动
- Vue3 还会结合静态提升、Patch Flag、Block Tree 缩小比较范围

### 2.5 为什么 `v-for` 不建议直接用 `index` 作为 `key`

- 列表顺序变动时，容易错误复用旧节点
- 可能导致组件内部状态错乱
- 会带来额外更新，影响性能和正确性

### 2.6 `v-if` 和 `v-for` 的区别

- `v-if` 是条件渲染，决定某个节点是否创建
- `v-for` 是循环渲染，决定渲染多少个节点
- 一般不建议把两者直接写在同一个元素上

### 2.7 MVVM 架构

MVVM 全称是 `Model - View - ViewModel`。

- `Model`：数据层，负责业务数据
- `View`：视图层，负责展示页面
- `ViewModel`：连接 View 和 Model，实现数据绑定和交互桥接

Vue 常被认为是典型的 MVVM 思想实践。

### 2.8 虚拟 DOM 简单示例

下面这个例子展示了如何用一个普通对象描述 DOM 结构，并递归渲染成真实 DOM：

```javascript
const vnode = {
  tag: 'div',
  attrs: { id: 'app', class: 'container' },
  children: [
    { tag: 'h1', attrs: {}, children: ['Hello'] },
    { tag: 'p', attrs: {}, children: ['Virtual DOM'] }
  ]
}

function render(vnode) {
  if (typeof vnode === 'string') {
    return document.createTextNode(vnode)
  }

  const el = document.createElement(vnode.tag)

  for (let key in vnode.attrs) {
    el.setAttribute(key, vnode.attrs[key])
  }

  vnode.children.forEach(child => {
    el.appendChild(render(child))
  })

  return el
}

document.body.appendChild(render(vnode))
```

## 3. Vue 组件通讯、插槽与状态管理

### 3.1 Vue 的组件通讯方式

1. `props` 和 `$emit`
2. `$parent` 和 `$children`
3. `ref` 和 `$refs`
4. EventBus 事件总线
5. `$attrs` 和 `$listeners`
6. Vuex
7. `provide` 和 `inject`
8. 作用域插槽

### 3.2 不同关系组件的常见选择

- 父子组件：`props`、`$emit`、`ref`
- 兄弟组件：事件总线、状态管理
- 祖孙组件：`provide/inject`
- 跨多组件共享数据：Vuex 或 Pinia

### 3.3 插槽

插槽的作用是让父组件把内容插入到子组件指定位置，提升组件灵活性和复用性。常见于按钮、卡片、弹窗、表格列等通用组件封装场景。

### 3.4 做过的组件二次封装

- 小程序：金刚区、瓷片区、轮播图、列表流、搜索栏
- 后台管理系统：左侧菜单栏、通用布局等

### 3.5 Pinia 和 Vuex 的区别

- Vuex 更常见于 Vue2 项目
- Pinia 更贴合 Vue3，语法更简洁
- Pinia 类型支持更友好，组合式风格更自然

### 3.6 Vuex 的状态组成

- `state`
- `actions`
- `mutations`
- `getters`
- `modules`

### 3.7 Pinia 的状态组成

- `state`
- `actions`
- `getters`

## 4. Vuex 中如何调用 getters

### 4.1 直接通过 `this.$store.getters` 访问

在组件的方法或计算属性中，可以直接通过 `this.$store.getters` 获取 getter 的返回值。

```javascript
export default {
  computed: {
    userDetail() {
      return this.$store.getters.getUserDetail
    }
  },
  created() {
    console.log(this.userDetail)
  }
}
```

也可以像这样直接使用：

```javascript
this.$store.getters.xxx
```

### 4.2 使用 `mapGetters` 辅助函数

`mapGetters` 可以把 store 中的 getters 映射为当前组件的计算属性，使用起来更简洁。

```javascript
import { mapGetters } from 'vuex'

export default {
  computed: {
    ...mapGetters(['someGetter'])
  }
}
```

上面例子中，`someGetter` 是 Vuex 中定义的 getter 名称，映射后可以直接通过 `this.someGetter` 访问。

### 4.2.1 带命名空间的 getter 调用

如果使用了模块化并开启了命名空间（`namespaced: true`），需要指定模块路径：

```javascript
import { mapGetters } from 'vuex'

export default {
  computed: {
    // 方式一：使用 mapGetters 指定命名空间
    ...mapGetters('user', ['userInfo', 'isLogin']),
    
    // 方式二：直接访问
    userInfo() {
      return this.$store.getters['user/userInfo']
    }
  }
}
```

### 4.3 用户列表示例

```vue
<template>
  <div>
    <ul>
      <li v-for="user in users" :key="user.id">
        {{ user.name }}
      </li>
    </ul>
  </div>
</template>

<script>
import { mapGetters } from 'vuex'

export default {
  computed: {
    ...mapGetters(['users'])
  }
}
</script>
```

### 4.4 用户详情示例

```vue
<template>
  <div>
    <h1>{{ user.name }}</h1>
    <p>{{ user.email }}</p>
  </div>
</template>

<script>
import { mapGetters } from 'vuex'

export default {
  props: {
    userId: {
      type: Number,
      required: true
    }
  },
  computed: {
    ...mapGetters(['getUserById']),
    user() {
      return this.getUserById(this.userId)
    }
  }
}
</script>
```

### 4.5 参数化 getters

getter 也可以返回一个函数，这样就能接收参数，适合做筛选类逻辑。

```javascript
// store.js
getters: {
  todosByStatus: (state) => (status) => {
    return state.todos.filter(todo => todo.done === status)
  }
}

// 组件中使用
computed: {
  doneTodos() {
    return this.$store.getters.todosByStatus(true)
  },
  pendingTodos() {
    return this.$store.getters.todosByStatus(false)
  }
}
```

## 5. Vue 生命周期与路由

### 5.1 Vue2 生命周期

- 创建阶段：`beforeCreate`、`created`
- 挂载阶段：`beforeMount`、`mounted`
- 更新阶段：`beforeUpdate`、`updated`
- 销毁阶段：`beforeDestroy`、`destroyed`
- 其他：`activated`、`deactivated`、`nextTick`

如果对应到 Vue3：

- `created` 前后的初始化逻辑很多会前移到 `setup`
- `beforeDestroy`、`destroyed` 变成 `onBeforeUnmount`、`onUnmounted`

### 5.2 父子组件生命周期执行顺序

父组件和子组件在挂载阶段的常见执行顺序是：

父 `beforeCreate` -> 父 `created` -> 父 `beforeMount` -> 子 `beforeCreate` -> 子 `created` -> 子 `beforeMount` -> 子 `mounted` -> 父 `mounted`

更新阶段通常是：

父 `beforeUpdate` -> 子 `beforeUpdate` -> 子 `updated` -> 父 `updated`

销毁阶段通常是：

父 `beforeDestroy` -> 子 `beforeDestroy` -> 子 `destroyed` -> 父 `destroyed`

### 5.3 路由守卫

- 全局前置守卫：`router.beforeEach`
- 全局解析守卫：`router.beforeResolve`
- 全局后置守卫：`router.afterEach`
- 路由独享守卫：`beforeEnter`
- 组件内守卫：`beforeRouteEnter`、`beforeRouteUpdate`、`beforeRouteLeave`

### 5.4 路由的两种传参方式

- `params`：通常配合 `name` 使用，刷新后可能丢失
- `query`：可以配合 `path` 或 `name` 使用，参数会展示在 URL 上

## 6. 小程序基础与常见业务流程

### 6.1 小程序生命周期

#### 应用生命周期

- `onLaunch`
- `onShow`
- `onHide`
- `onError`

#### 页面生命周期

- `onLoad`
- `onShow`
- `onReady`
- `onUnload`
- `onResize`
- `onPullDownRefresh`
- `onInit`

### 6.2 小程序如何跳转到 Web 页面

最常见的是使用 `web-view` 组件。

#### 方式一：直接在页面中使用 `web-view`

```html
<web-view src="https://www.example.com"></web-view>
```

使用前要先在小程序后台配置业务域名。

#### 方式二：uni-app 中单独创建 WebView 页面

1. 在 `pages.json` 中配置域名白名单：

```json
{
  "mp-weixin": {
    "web-view": {
      "domain": ["https://your-domain.com"]
    }
  }
}
```

2. 创建页面组件：

```vue
<template>
  <web-view :src="webUrl"></web-view>
</template>

<script>
export default {
  data() {
    return {
      webUrl: ''
    }
  },
  onLoad(options) {
    this.webUrl = decodeURIComponent(options.url)
  }
}
</script>
```

3. 跳转代码：

```javascript
uni.navigateTo({
  url: `/pages/webview/webview?url=${encodeURIComponent('https://example.com')}`
})
```

### 6.3 微信小程序跳转到另一个小程序

#### 方式一：通过 `appId` 和页面路径跳转

```javascript
wx.navigateToMiniProgram({
  appId: '目标小程序appid',
  path: '目标小程序页面路径',
  envVersion: 'release',
  success(res) {
    console.log('跳转小程序成功！', res)
  }
})
```

#### 方式二：通过 `shortLink` 跳转

```javascript
wx.navigateToMiniProgram({
  shortLink: '目标小程序链接',
  envVersion: 'release',
  success(res) {
    console.log('跳转小程序成功！', res)
  }
})
```

#### uni-app 写法

```javascript
uni.navigateToMiniProgram({
  appId: '目标小程序的 appid',
  path: 'pages/index/index?id=123',
  extraData: {
    foo: 'bar'
  },
  envVersion: 'release',
  success(res) {
    console.log('跳转成功')
  },
  fail(err) {
    console.log('跳转失败', err)
  }
})
```

#### 注意事项

- 目标小程序需要在配置中允许来源 `appId`
- 一般要求用户主动触发
- 可通过 `extraData` 传参

### 6.4 微信小程序跳转公众号

#### 方式一：关联公众号后使用官方组件

- 公众号后台先关联小程序
- 小程序后台开启“引导关注公众号”
- 页面中使用 `<official-account></official-account>` 组件

#### 方式二：企业号场景下结合 `web-view`

已认证企业号在满足平台条件时，也可以通过 `web-view` 打开关联的公众号内容页。

#### 公众号跳第三方小程序

可通过 `<wx-open-launch-weapp>` 组件实现：

```html
<wx-open-launch-weapp
  @launch="handleLaunch"
  @error="handleError"
  id="launch-btn"
  appid="第三方的 appid"
  path="第三方小程序的跳转路径"
  style="display: block;"
>
  <button>跳转公众号相关功能</button>
</wx-open-launch-weapp>
```

### 6.5 长按识别图片二维码

#### WXML 示例

```html
<view bindlongpress="longPressHandler">
  <image
    src="图片路径"
    data-src="图片路径"
    show-menu-by-longpress="{{true}}"
  />
</view>
```

#### JS 示例

```javascript
longPressHandler: function(e) {
  const src = e.currentTarget.dataset.src
  wx.scanCode({
    scanType: ['qrCode'],
    success: (res) => {
      console.log(res.result)
    }
  })
}
```

#### 权限声明

```javascript
"permission": {
  "scope.scanCode": {
    "desc": "扫码"
  }
}
```

### 6.6 微信小程序第三方登录流程

#### 前端流程

1. 调用 `wx.login()` 获取临时凭证 `code`
2. 将 `code` 发送给后端

#### 后端流程

1. 后端使用 `code` 调微信接口换取 `openid` 和 `session_key`
2. 进行用户绑定或注册
3. 生成自定义登录态，例如 JWT 或 `sessionId`
4. 返回给前端保存并参与后续接口调用

### 6.7 小程序支付流程

1. 用户点击支付，前端调用接口请求统一下单
2. 后端调用微信统一下单接口，生成 `prepay_id`
3. 后端把支付参数返回给前端
4. 前端通过 `uni.requestPayment` 拉起支付
5. 微信支付成功后，会异步通知后端
6. 后端验签成功后更新订单状态

### 6.8 微信小程序退款流程

基础流程通常包括：

1. 申请企业小程序并完成微信认证
2. 申请商户号并接入微信支付
3. 在小程序后台关联商户号

如果后续还要扩展，可以继续补充退款接口、证书配置、回调通知和异常处理。

## 7. WebSocket、长轮询与网络基础

### 7.1 WebSocket 是什么

WebSocket 是一种支持客户端和服务端双向通信的协议。不同于 HTTP 一问一答的请求响应模式，WebSocket 建立连接后，服务端也可以主动推送消息给客户端。

### 7.2 WebSocket 常用方法

- `onopen`
- `onmessage`
- `onclose`
- `onerror`
- `send`
- `close`

### 7.3 WebSocket 心跳机制

为了保证连接可用，客户端通常会每隔一段时间发送一个心跳包，服务器收到后返回响应。若一定时间内没有收到响应，通常会认为连接已断开，并触发重连逻辑。

#### 心跳机制实现示例

```javascript
let heartbeatTimer = null
let reconnectTimer = null
let missedHeartbeats = 0
const MAX_MISSED_HEARTBEATS = 3

const ws = new WebSocket('ws://example.com')

ws.onopen = () => {
  console.log('WebSocket 连接成功')
  missedHeartbeats = 0
  
  // 每 30 秒发送一次心跳
  heartbeatTimer = setInterval(() => {
    if (ws.readyState === WebSocket.OPEN) {
      ws.send(JSON.stringify({ type: 'ping' }))
      missedHeartbeats++
      
      // 如果连续多次未收到响应，认为连接断开
      if (missedHeartbeats >= MAX_MISSED_HEARTBEATS) {
        console.log('心跳超时，准备重连')
        ws.close()
      }
    }
  }, 30000)
}

ws.onmessage = (event) => {
  const data = JSON.parse(event.data)
  
  if (data.type === 'pong') {
    // 收到心跳响应，重置计数器
    missedHeartbeats = 0
    console.log('心跳响应正常')
  } else {
    // 处理业务消息
    console.log('收到消息:', data)
  }
}

ws.onclose = () => {
  console.log('WebSocket 连接关闭')
  clearInterval(heartbeatTimer)
  
  // 5 秒后尝试重连
  reconnectTimer = setTimeout(() => {
    console.log('尝试重新连接...')
    // 这里可以递归调用连接函数
  }, 5000)
}

ws.onerror = (error) => {
  console.error('WebSocket 错误:', error)
}
```

### 7.4 单向通信方案

如果不使用 WebSocket，但需要服务端有数据时再返回，可以考虑 HTTP 长轮询。

基本思路：

1. 客户端发起请求
2. 服务端暂时挂起连接，直到有数据可返回
3. 客户端拿到结果后立刻再次发起请求

### 7.5 浏览器跨域问题

当协议、域名、端口有一个不同，就会产生跨域问题。常见解决方案有：

- 后端设置 CORS
- 前端开发环境配置代理
- 使用 Nginx 反向代理
- JSONP

### 7.6 浏览器三次握手和四次挥手

#### 三次握手

1. 客户端发送 SYN，请求建立连接
2. 服务端返回 SYN + ACK，表示收到并同意连接
3. 客户端再发送 ACK，连接建立完成

#### 四次挥手

1. 客户端发送 FIN，表示要关闭连接
2. 服务端返回 ACK，确认收到
3. 服务端处理完剩余数据后，再发送 FIN
4. 客户端返回 ACK，连接彻底关闭

### 7.7 前端防止泄密的常见措施

- 使用 `HTTPS`
- 使用 `Token` 做鉴权
- 避免把敏感信息长时间存到 `localStorage` 或 `sessionStorage`
- 代码混淆、资源压缩、文件名哈希

## 8. JavaScript 基础高频题

### 8.1 宏任务和微任务

JavaScript 运行机制基于事件循环。

- 宏任务：`setTimeout`、`setInterval`、I/O、`requestAnimationFrame`
- 微任务：`Promise.then`、`catch`、`finally`、`async/await`

微任务会在当前宏任务执行结束后、下一个宏任务开始前优先执行。

### 8.2 JS 的数据类型

#### 基本数据类型

- `Number`
- `String`
- `Boolean`
- `Undefined`
- `Null`

#### 引用数据类型

- `Object`
- `Array`
- `Function`
- `Date`

### 8.3 `null` 和 `undefined` 的区别

- `undefined`：声明了变量但没有赋值，或者函数没有返回值
- `null`：人为赋的空值，表示当前没有对象或内容

### 8.4 对象转成数组的方法

- `Object.keys(obj)`
- `Object.values(obj)`
- `for...in`

### 8.5 `String` 转 `Number`

- `Number(str)`
- `+str`
- `parseInt(str)`
- `parseFloat(str)`

### 8.6 闭包及缺点

闭包指函数可以访问其外部函数作用域中的变量。

**优点**：
- 可以延长变量生命周期
- 实现数据封装和私有变量
- 适合模块化开发

**缺点**：
- 如果闭包引用的外部变量一直不被释放（如事件监听器未移除、定时器未清除），会导致内存无法回收，形成内存泄漏
- 过度使用闭包会增加内存占用

### 8.7 原型和原型链

每个函数都有一个 `prototype` 属性，对应一个原型对象；对象在查找属性时，如果自身没有，就会沿着原型继续向上查找，这个查找链路就叫原型链。

![原型链示意图](https://cdn.nlark.com/yuque/0/2025/png/50923934/1749822033350-bd0b3ef9-b3e5-4e1a-b5d9-0734fd0bc6be.png)

### 8.8 深拷贝和浅拷贝

- 浅拷贝：只复制第一层，更深层对象依旧共享引用
- 深拷贝：递归复制各层数据，新旧对象互不影响

常见方式：

- 浅拷贝：`slice()`、`concat()`、`Object.assign()`
- 深拷贝：`JSON.parse(JSON.stringify(obj))`、递归、自定义方法、`_.cloneDeep`

### 8.9 防抖和节流

#### 防抖

```javascript
function debounce(fn, delay) {
  let timer = null
  return function(...args) {
    clearTimeout(timer)
    timer = setTimeout(() => {
      fn.apply(this, args)
    }, delay)
  }
}
```

#### 节流

```javascript
function throttle(fn, interval) {
  let lastTime = 0
  return function(...args) {
    const now = Date.now()
    if (now - lastTime >= interval) {
      lastTime = now
      fn.apply(this, args)
    }
  }
}
```

## 9. CSS、布局与浏览器渲染

### 9.1 Flexbox 中 `flex: 1` 的含义

`flex: 1` 常被理解为以下三个属性的简写：

- `flex-grow: 1`
- `flex-shrink: 1`
- `flex-basis: 0`

它的常见效果是：

- 尽量占据剩余空间
- 空间不足时允许收缩
- 更偏向按比例分配而不是优先看内容本身大小

### 9.2 `flex: 1` 和 `flex: auto` 的区别

- `flex: 1` 等价于 `flex: 1 1 0`
- `flex: auto` 等价于 `flex: 1 1 auto`

区别在于：

- `flex: 1` 更强调平均分配
- `flex: auto` 会更参考内容初始尺寸

### 9.3 如何将 `1px` 转成 `0.5px`

可以利用 CSS 的缩放能力模拟细线：

```css
.half-px {
  background-color: #000;
  height: 1px;
  transform: scale(0.5);
  transform-origin: 0 0;
}
```

### 9.4 回流和重绘

- 回流：元素尺寸、位置、结构变化引起布局重新计算
- 重绘：只改变外观，不影响布局

结论：

- 回流一定会触发重绘
- 重绘不一定会触发回流
- 回流的性能开销通常更大

## 10. ECharts、2D 页面与可视化

### 10.1 ECharts 图表常用属性

- `series`：系列列表，决定图表类型和数据
- `xAxis`：x 轴配置
- `yAxis`：y 轴配置
- `grid`：直角坐标系网格区域
- `title`：标题组件
- `tooltip`：提示框组件
- `legend`：图例组件
- `color`：颜色配置

### 10.2 ECharts 柱状图倒置的两种方法

常见有两种思路：

- **方法一（推荐）**：通过 `yAxis.inverse = true` 直接反转 Y 轴
- **方法二**：通过 `reverse()` 调整数据顺序，实现倒序展示

#### 方法一：Y 轴倒置（推荐）

这种方式最简单直接，只需要设置 `yAxis.inverse = true`：

```javascript
var chartDom = document.getElementById('main')
var myChart = echarts.init(chartDom)
var option = {
  xAxis: {
    type: 'category',
    data: ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun']
  },
  yAxis: {
    type: 'value',
    inverse: true  // 关键配置：Y 轴倒置
  },
  series: [{
    data: [120, 200, 150, 80, 70, 110, 130],
    type: 'bar'
  }]
}

myChart.setOption(option)
```

#### 方法二：调整数据顺序

通过反转数据数组来实现倒序展示：

```javascript
var chartDom = document.getElementById('main')
var myChart = echarts.init(chartDom)
var originalData = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun']
var originalValues = [120, 200, 150, 80, 70, 110, 130]

// 反转数据
var reversedData = originalData.slice().reverse()
var reversedValues = originalValues.slice().reverse()

var option = {
  xAxis: {
    type: 'category',
    data: reversedData
  },
  yAxis: {
    type: 'value'
  },
  series: [{
    data: reversedValues,
    type: 'bar'
  }]
}

myChart.setOption(option)
```

#### 综合示例：X 轴置顶 + Y 轴倒置

如果需要横向柱状图并且倒置，可以这样配置：

```javascript
var chartDom = document.getElementById('main')
var myChart = echarts.init(chartDom)
var option = {
  xAxis: {
    type: 'value',
    position: 'top'  // X 轴置顶
  },
  yAxis: {
    type: 'category',
    data: ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'],
    inverse: true  // Y 轴倒置
  },
  series: [{
    type: 'bar',
    data: [120, 200, 150, 80, 70, 110, 130]
  }]
}

myChart.setOption(option)
```

### 10.3 2D 页面常用技术

- `Canvas`：适合绘图、游戏、动画、图表
- `SVG`：适合矢量图、流程图、图标
- `CSS`：适合常规布局、样式和简单动画

## 11. Three.js、数字孪生与 3D 项目表达

### 11.1 常见 3D 技术

- `Three.js`：通用 3D 场景、模型展示、交互动画
- `Cesium.js`：偏地理空间 3D 可视化

### 11.2 3D 人物模型常见实现点

- 加载 `glTF`、`FBX` 等模型
- 配置相机、光源、材质
- 使用 `Raycaster` 实现点击与拾取
- 结合动画系统处理姿态和动作

### 11.3 Three.js 中常见动画方式

- `requestAnimationFrame`
- `THREE.AnimationMixer`
- `tween.js`
- 材质与颜色动画
- 粒子系统动画
- 相机动画
- 基于 GLSL 的着色器动画

### 11.4 搭建 Three.js 的基本步骤

1. 安装 Three.js
2. 导入核心模块
3. 创建场景 `scene`
4. 创建相机 `camera`
5. 创建渲染器 `renderer`
6. 添加物体
7. 编写动画循环

### 11.5 数字孪生项目常见功能

- 设备状态实时展示
- 3D 场景还原
- 热力图、流向图
- 数据面板联动

### 11.6 常见检测数据

- 温度、湿度、烟雾浓度
- 电流、电压、功率、运行状态
- 人员定位、轨迹信息

### 11.7 项目中可以这样表达

#### 负责模块

- 3D 场景搭建与渲染
- 交互逻辑开发
- 数据可视化面板联动

#### 成果描述

成功将 3D 可视化和业务数据打通，提升了展示效果、复用性和可维护性，也让业务决策更直观。

## 12. 工程化、优化与项目经验

### 12.1 Tree Shaking

Tree Shaking 指在打包阶段移除未使用的代码，从而减小产物体积，优化加载性能。

### 12.2 项目的打包和优化

#### 打包

通常通过 `npm run build` 打包项目。

#### 常见优化

- CDN 加速
- Gzip 或 Brotli 压缩
- 图片压缩
- 代码压缩
- 资源懒加载
- 组件按需加载
- 路由拆分
- 合理缓存
- 防抖节流

### 12.3 前端优化思路

- 开启压缩
- 懒加载资源
- 减少首屏体积
- 合理使用缓存
- 使用生产模式构建
- 尽量减少回流和重绘

### 12.4 做过的权限功能

- 菜单权限：通过动态路由按角色加载菜单
- 按钮权限：通过自定义指令控制按钮显隐或禁用

### 12.5 Git 上传代码流程

1. `git init`
2. `git remote add origin <远程仓库地址>`
3. `git status`
4. `git add .`
5. `git commit -m "提交说明"`
6. `git push -u origin master`
7. `git branch -a`
8. `git checkout <分支名>`

### 12.6 项目从 0 到 1 的流程

1. 需求对接与评审
2. 技术选型
3. 项目初始化
4. 基础模块开发
5. 业务功能开发
6. 联调测试
7. 性能优化
8. 部署上线
