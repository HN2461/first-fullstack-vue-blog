# 完整增量发布实操记录

本文档用于记录当前这种“前端改了、后端改了、数据库结构也可能动了”的完整增量发布过程。

它和《二次部署更新实操记录》的区别是：

- 二次部署更新：适合前端或后端小改，数据库没有结构风险。
- 完整增量发布：适合前端、后端、数据库相关逻辑都有变化，需要先备份、再发布、最后验证。

## 核心原则

本次发布按下面顺序执行：

```text
本地验证
  -> 本地打包前端
  -> 本地打包干净后端
  -> 上传发布包
  -> 服务器备份数据库、前端、后端、上传文件
  -> 发布前端
  -> 发布后端
  -> 视情况执行数据库迁移
  -> 重启后端
  -> 验证页面和接口
```

为什么要这样做：

- 先本地验证，是为了在上传前发现构建、测试、编码问题。
- 先备份服务器，是为了发布失败时可以回滚。
- 前端和后端分开打包，是因为它们是两个独立应用。
- 后端发布包不能包含 `.env`、`node_modules`、`uploads`，避免覆盖服务器环境配置和线上用户数据。
- 数据库结构变化不能直接覆盖线上库，除非明确要用本地数据库替换线上数据库。

## 2026-06-23 成功发布案例：账本模块完整增量发布

本节记录 2026-06-23 账本模块发布的完整成功案例。后续如果再次要求“直接部署 / 发布前后端和数据库”，优先按本节执行。

### 固定服务器与线上目录

当前线上服务器：

```text
公网 IP：121.41.11.78
登录用户：root
线上访问地址：http://121.41.11.78/
健康检查：http://121.41.11.78/api/health
```

注意：

```text
https://121.41.11.78/ 当前不可用。
服务器 443 端口未启用，Nginx 中 SSL listen / ssl_certificate 仍是注释状态。
除非后续专门配置 HTTPS 证书，否则发布验证使用 http://121.41.11.78/。
```

固定部署目录：

```text
/www/personal-blog/frontend
/www/personal-blog/backend
/www/personal-blog/uploads
/www/personal-blog/backups
```

固定 PM2 进程：

```text
personal-blog-api
```

固定数据库：

```text
MongoDB URI：mongodb://127.0.0.1:27017/personal_fullstack_blog
```

安全规则：

```text
服务器密码只允许由用户临时提供，不写入仓库、文档、脚本或聊天总结。
生产 .env 由服务器维护，发布包不能覆盖。
uploads 是线上运行数据，发布时只备份，不覆盖。
数据库默认只备份，不做整库恢复；只有用户明确要求用本地库替换线上库时才允许 mongorestore --drop。
```

### 本次发布范围

本次发布包含：

```text
前端：账本多页面模块、汇总图表、流水明细、日表格、重要记录、分类/导入弹窗入口。
后端：ledger 模块、模型、路由、服务、校验、RBAC 菜单种子、权限隔离测试。
数据库：新增集合由线上 MongoDB 按使用自动创建；账本菜单由后端启动时 ensureRbacSeed 自动同步入库。
```

本次数据库处理结论：

```text
不需要 mongorestore。
不需要手写迁移脚本。
需要发布前 mongodump 备份。
需要重启后端，让 ensureRbacSeed 同步 knowledge.ledger 菜单。
```

### 本地发布前验证

先完整验证：

```powershell
cd frontend
npm run build
cd ..\backend
npm run test
cd ..
pwsh -File scripts\check-encoding.ps1
```

2026-06-23 验证结果：

```text
frontend npm run build：通过，仅有 Vite chunk size warning。
backend npm run test：14 个测试文件、122 个用例通过。
scripts/check-encoding.ps1：通过，未发现 UTF-8 BOM。
```

### 打包前必须清理前端 dist

这次发布前发现 `frontend/dist` 曾残留历史构建产物，导致前端 zip 异常膨胀到 284MB，并包含旧的 `LedgerCategoriesPage` / `LedgerImportsPage` chunk。

正确做法是在仓库根目录先删除 `frontend/dist`，再重新构建：

```powershell
$distPath = Join-Path (Resolve-Path .).Path 'frontend\dist'
if (Test-Path -LiteralPath $distPath) {
  Remove-Item -LiteralPath $distPath -Recurse -Force
}
Push-Location frontend
npm run build
Pop-Location
```

清理后检查：

```powershell
Get-ChildItem frontend\dist\assets |
  Where-Object { $_.Name -match 'Ledger(Categories|Imports)Page' } |
  Select-Object Name,Length,LastWriteTime

Get-ChildItem frontend\dist\assets |
  Measure-Object Length -Sum |
  Select-Object Count,Sum
```

2026-06-23 清理后结果：

```text
旧分类/导入页面 chunk：无残留。
frontend/dist/assets：256 个文件，总大小约 8.49MB。
```

### 生成发布包

统一打包：

```powershell
pwsh -File scripts\package-release.ps1
```

2026-06-23 最终发布包：

```text
release/frontend-dist.zip：2714988 bytes，约 2.6MB
release/backend-release.zip：238155 bytes，约 233KB
```

后端发布包抽查应包含账本模块，且不包含 `.env`、`node_modules`、`uploads`：

```powershell
Add-Type -AssemblyName System.IO.Compression.FileSystem
$zip = 'release\backend-release.zip'
[IO.Compression.ZipFile]::OpenRead((Resolve-Path $zip)).Entries |
  Where-Object { $_.FullName -match 'ledger|\.env$|node_modules|uploads' } |
  Select-Object -First 80 -ExpandProperty FullName
```

2026-06-23 抽查结果包含：

```text
src/modules/ledger/models/LedgerBook.js
src/modules/ledger/models/LedgerCategory.js
src/modules/ledger/models/LedgerEntry.js
src/modules/ledger/models/LedgerImportBatch.js
src/modules/ledger/models/LedgerMoment.js
src/modules/ledger/routes/ledger.routes.js
src/modules/ledger/services/ledger.service.js
src/modules/ledger/services/ledgerMoment.service.js
src/modules/ledger/validators/ledger.validator.js
tests/ledger.test.js
```

### 上传方式

如果本机有可用 SSH 密钥或用户临时提供 SSH 密码，可以直接上传到服务器：

```text
/www/personal-blog/backups/frontend-dist.zip
/www/personal-blog/backups/backend-release.zip
```

2026-06-23 本机没有 `sshpass` / `plink` / `pscp`，但 Python 环境已有 `paramiko`，最终使用 Paramiko 通过 SFTP 上传。

密码不得写入文档。后续如需自动化，可在当前 PowerShell 临时设置环境变量，再由 Python 读取：

```powershell
$env:DEPLOY_HOST = '121.41.11.78'
$env:DEPLOY_USER = 'root'
$env:DEPLOY_PASSWORD = '<由用户临时输入，不写入文件>'
```

上传后服务器检查：

```bash
ls -lh /www/personal-blog/backups/frontend-dist.zip /www/personal-blog/backups/backend-release.zip
```

2026-06-23 服务器检查：

```text
/www/personal-blog/backups/frontend-dist.zip  2.6M
/www/personal-blog/backups/backend-release.zip 233K
```

### 服务器发布脚本

上传成功后，在服务器执行下面脚本。该脚本会先备份 MongoDB、前端、后端、uploads 和生产 `.env`，备份成功后再替换前后端。

```bash
set -euo pipefail

RELEASE_DIR=/www/personal-blog/backups/release-$(date +%Y%m%d-%H%M%S)
mkdir -p "$RELEASE_DIR"
echo "RELEASE_DIR=$RELEASE_DIR"

echo "[1/9] MongoDB backup"
mongodump --uri="mongodb://127.0.0.1:27017/personal_fullstack_blog" --out="$RELEASE_DIR/mongodb-before"
test -d "$RELEASE_DIR/mongodb-before/personal_fullstack_blog"

echo "[2/9] File backups"
cp -a /www/personal-blog/frontend "$RELEASE_DIR/frontend-before"
cp -a /www/personal-blog/backend "$RELEASE_DIR/backend-before"
cp -a /www/personal-blog/uploads "$RELEASE_DIR/uploads-before"
test -f /www/personal-blog/backend/.env
cp /www/personal-blog/backend/.env "$RELEASE_DIR/backend.env.before-release"

echo "[3/9] Publish frontend"
rm -rf /www/personal-blog/frontend/*
unzip -oq /www/personal-blog/backups/frontend-dist.zip -d /www/personal-blog/frontend
test -f /www/personal-blog/frontend/index.html

echo "[4/9] Publish backend"
OLD_BACKEND=/www/personal-blog/backend_old_$(date +%Y%m%d_%H%M%S)
mv /www/personal-blog/backend "$OLD_BACKEND"
mkdir -p /www/personal-blog/backend
unzip -oq /www/personal-blog/backups/backend-release.zip -d /www/personal-blog/backend
cp "$RELEASE_DIR/backend.env.before-release" /www/personal-blog/backend/.env
chmod 600 /www/personal-blog/backend/.env
test -f /www/personal-blog/backend/package.json

echo "OLD_BACKEND=$OLD_BACKEND"

echo "[5/9] Install backend dependencies"
cd /www/personal-blog/backend
npm install --omit=dev

echo "[6/9] Restart PM2"
pm2 restart personal-blog-api --update-env

echo "[7/9] PM2 status"
pm2 jlist | node -e "let s=''; process.stdin.on('data',d=>s+=d); process.stdin.on('end',()=>{const apps=JSON.parse(s); const app=apps.find(a=>a.name==='personal-blog-api'); if(!app){console.error('PM2 app missing'); process.exit(2)} console.log(JSON.stringify({name:app.name,status:app.pm2_env.status,restarts:app.pm2_env.restart_time,pid:app.pid}, null, 2)); if(app.pm2_env.status!=='online') process.exit(3)})"

echo "[8/9] Local health"
for i in 1 2 3 4 5; do
  if curl -fsS http://127.0.0.1:3001/api/health; then echo; break; fi
  sleep 2
  if [ "$i" = "5" ]; then exit 4; fi
done

echo "[9/9] Save PM2 and sizes"
pm2 save
ls -lh /www/personal-blog/frontend/index.html /www/personal-blog/backend/package.json /www/personal-blog/backups/frontend-dist.zip /www/personal-blog/backups/backend-release.zip
echo "DONE_RELEASE_DIR=$RELEASE_DIR"
```

注意：

```text
PM2 重启后，健康检查前 1-2 次 curl 可能短暂 Connection refused。
这属于 Node 服务刚重启的窗口期，只要后续重试返回 success:true 即可。
```

### 2026-06-23 服务器发布结果

本次发布备份目录：

```text
/www/personal-blog/backups/release-20260623-234749
```

旧后端目录保留为：

```text
/www/personal-blog/backend_old_20260623_234801
```

PM2 状态：

```json
{
  "name": "personal-blog-api",
  "status": "online",
  "pid": 61390,
  "restarts": 10
}
```

健康检查：

```text
http://127.0.0.1:3001/api/health：success:true
http://121.41.11.78/api/health：success:true
http://121.41.11.78/：200，标题为“个人全栈博客系统”
```

后端日志关键行：

```text
API server listening on http://127.0.0.1:3001
GET /api/health 200
```

服务器文件检查：

```text
/www/personal-blog/backend/src/modules/ledger：9 个文件
/www/personal-blog/frontend/assets：账本相关 chunk 已存在
```

### 本次菜单入库结果

后端启动时会执行 `ensureRbacSeed()`，本次发布后线上 MongoDB 已同步账本菜单。

检查命令：

```bash
mongosh --quiet mongodb://127.0.0.1:27017/personal_fullstack_blog --eval "db.menus.find({code:/^knowledge\\.ledger/},{code:1,name:1,routePath:1,hidden:1,sortOrder:1,_id:0}).sort({sortOrder:1}).toArray()"
```

2026-06-23 结果：

```text
knowledge.ledger.overview：汇总图表，/console/ledger/overview，显示
knowledge.ledger.entries：流水明细，/console/ledger/entries，显示
knowledge.ledger：账本，分组菜单，显示
knowledge.ledger.daily：日表格，/console/ledger/daily，显示
knowledge.ledger.moments：重要记录，/console/ledger/moments，显示
knowledge.ledger.categories：分类字段，隐藏能力菜单
knowledge.ledger.imports：导入记录，隐藏能力菜单
```

### 本次非阻断风险

服务器 `npm install --omit=dev` 后提示：

```text
3 vulnerabilities：2 moderate，1 high
```

结论：

```text
该提示不影响本次服务启动和发布成功。
不要在发布窗口直接执行 npm audit fix --force，因为它可能升级破坏性版本。
后续应单独安排依赖安全升级任务。
```

### 下次部署快速清单

下次用户要求部署时，按下面顺序执行：

```text
1. 本地 git status，确认工作区变更范围，只记录不回退用户改动。
2. 本地运行 frontend build、backend test、check-encoding。
3. 删除 frontend/dist 后重新 npm run build，避免历史 chunk 残留。
4. 执行 scripts/package-release.ps1。
5. 抽查 release/frontend-dist.zip 和 release/backend-release.zip。
6. 上传到 121.41.11.78:/www/personal-blog/backups/。
7. 服务器 mongodump 备份 MongoDB。
8. 备份 frontend、backend、uploads、backend/.env。
9. 替换 frontend。
10. 替换 backend，并恢复服务器 .env。
11. cd /www/personal-blog/backend && npm install --omit=dev。
12. pm2 restart personal-blog-api --update-env。
13. pm2 save。
14. 验证 http://121.41.11.78/ 和 http://121.41.11.78/api/health。
15. 若有菜单/权限种子变化，查 db.menus 确认 ensureRbacSeed 已同步。
16. 记录本次备份目录、发布包大小、PM2 状态和已知风险。
```

## 2026-06-19 本次发布实操结果

### 发布范围

本次发布包含：

- 前端页面、样式和接口调用更新。
- 后端代码更新。
- 后端发布流程固化：新增 `scripts/package-release.ps1`，统一生成发布包。
- 发布文档更新：补充完整增量发布、前端热修复、日志复查、后端文件放置规则。
- 登录兼容修复：解决 `http://服务器IP` 访问时浏览器不支持 `window.crypto.subtle` 导致登录页报 `importKey` 的问题。
- 前端参数修复：解决 `/api/rbac/roles?page=1&pageSize=200` 超过后端校验限制导致 `400` 的问题。

### 本次发布包

本地统一打包命令：

```powershell
pwsh -File scripts\package-release.ps1
```

生成发布包：

```text
release/frontend-dist.zip
release/backend-release.zip
```

本次最终前端热修复包大小：

```text
147228952
```

服务器上传位置：

```text
/www/personal-blog/backups/frontend-dist.zip
/www/personal-blog/backups/backend-release.zip
```

### 本次发布前备份

本次备份目录：

```text
/www/personal-blog/backups/release-20260619-before-full-update
```

备份内容：

```text
backend-before
backend.env.before-release
frontend-before
mongodb-before
uploads-before
```

空间检查结果：

```text
/dev/vda3 总计 49G，已用 11G，可用 37G，使用率 23%
/www/personal-blog 占用约 1.7G
/www/personal-blog/backups 占用约 745M
本次 release-20260619-before-full-update 占用约 595M
```

结论：

```text
当前磁盘空间不紧张。本次备份暂不清理，等新版本稳定后再清理旧备份。
```

### 本次服务器发布过程

前端发布成功，最终正式目录检查：

```text
/www/personal-blog/frontend
  assets
  favicon.svg
  index.html
```

后端发布成功，正式目录检查：

```text
/www/personal-blog/backend
  .env
  .env.example
  ecosystem.config.cjs
  package.json
  package-lock.json
  src
  tests
  README.md
  vitest.config.js
```

后端依赖安装成功：

```text
node_modules 已在服务器重新生成
```

PM2 重启成功：

```text
personal-blog-api online
```

健康检查通过：

```text
curl http://127.0.0.1:3001/api/health
curl http://127.0.0.1/api/health
```

两者均返回：

```json
{"success":true}
```

### 本次中途问题 1：HTTP IP 登录页 importKey 报错

现象：

```text
Cannot read properties of undefined (reading 'importKey')
```

原因：

```text
当前通过 http://8.146.236.23 访问，属于非 HTTPS 的公网 IP。
浏览器在该环境下可能不提供 window.crypto.subtle。
旧前端登录流程强制调用 window.crypto.subtle.importKey，导致登录页报错。
```

处理：

```text
前端新增浏览器加密能力判断。
HTTPS / localhost / 支持 Web Crypto 时继续使用浏览器内密码加密。
HTTP IP / 不支持 crypto.subtle 时，临时走后端已有普通认证接口，并通过 Bearer token 维持登录态。
```

验证：

```text
重新构建并发布前端后，登录页可正常登录。
```

后续建议：

```text
正式生产环境应配置 HTTPS，不长期依赖 HTTP IP 兼容模式。
```

### 本次中途问题 2：RBAC 角色接口 pageSize=200 返回 400

现象：

```text
GET /api/rbac/roles?page=1&pageSize=200 400
```

原因：

```text
后端角色查询接口限制 pageSize 最大值为 100。
前端 collectAllPageItems 默认使用 pageSize=200，超过后端校验范围。
```

处理：

```text
将 frontend/src/services/admin.js 中 collectAllPageItems 默认 pageSize 从 200 调整为 100。
```

验证：

发布前端热修复后，日志变为：

```text
GET /api/rbac/roles?page=1&pageSize=100 200
```

### 本次发布后日志结论

正常日志：

```text
POST /api/auth/login 200
GET /api/auth/me 200
GET /api/public/knowledge-menu 200
GET /api/admin/articles?page=1&pageSize=10 200
GET /api/admin/categories?page=1&pageSize=100 200
GET /api/admin/comments?status=pending&page=1&pageSize=15 304
GET /api/admin/users?page=1&pageSize=10 200
GET /api/rbac/roles?page=1&pageSize=100 200
```

非阻塞遗留问题：

```text
GET /uploads/avatars/...jpg 404
GET /uploads/2026/06/...png 404
```

判断：

```text
这是历史上传资源文件缺失或上传目录迁移不完整问题，不阻塞本次代码发布。
后续可单独做“上传资源迁移 / 缺失资源修复”。
```

### 本次发布结论

```text
本次完整增量发布成功。
前端、后端、Nginx 代理、PM2、核心接口均已验证可用。
本次发布前备份保留在 /www/personal-blog/backups/release-20260619-before-full-update。
后续运行稳定后，再按备份清理策略删除旧 release 目录或下载到本地归档。
```

## 本次发布信息

### 发布时间

```text
待填写
```

### 发布人

```text
待填写
```

### 发布范围

本次发布涉及：

- [ ] 前端页面 / 样式
- [ ] 前端接口调用
- [ ] 后端接口
- [ ] 后端数据模型
- [ ] 数据库字段或数据结构
- [ ] 权限 / 菜单 / 角色
- [ ] 上传文件或媒体资源
- [ ] Nginx 配置
- [ ] PM2 配置

### 本次主要变更说明

```text
待填写
```

### 数据库变化判断

先判断本次数据库变化属于哪一种：

- [ ] 只新增非必填字段，旧数据为空也能正常运行。
- [ ] 新增字段并且旧数据必须补默认值。
- [ ] 字段改名。
- [ ] 字段类型变化。
- [ ] 枚举值变化。
- [ ] 角色、权限、菜单结构变化。
- [ ] 分类、文章、媒体等业务关系变化。
- [ ] 不确定，需要先备份并发布后观察接口日志。

结论：

```text
待填写：是否需要数据库迁移脚本；如果需要，脚本文件名是什么。
```

## 第 1 步：本地确认工作区状态

### 目标

发布前先确认本地代码处于可控状态，知道哪些文件发生了变化。

### 为什么要做

如果本地还有不确定的临时改动，直接打包会把半成品一起发到服务器。先看一眼 Git 状态，可以避免把无关修改混进发布包。

### 你现在要做什么

在本机项目根目录执行：

```powershell
git status --short
```

如果想看更完整的变更文件列表：

```powershell
git status
```

### 执行后怎么检查

重点看：

- 是否有不准备发布的临时文件。
- 是否有 `.env`、`node_modules`、`dist`、上传文件等不该提交或不该打包的内容。
- 本次变更是否和你预期一致。

### 预期结果

能明确知道本次要发布哪些代码变更。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 2 步：本地执行前端构建

### 目标

确认前端代码可以正常生产构建，并生成最新的静态资源。

### 为什么要做

服务器上 Nginx 托管的是前端构建产物，不是 Vue 源码。只改了 `.vue`、`.js`、`.css` 文件但不重新构建，服务器页面不会变。

### 你现在要做什么

在本机项目根目录执行：

```powershell
cd frontend
npm run build
```

### 执行后怎么检查

构建完成后执行：

```powershell
dir dist
```

重点看：

- 是否生成 `dist` 目录。
- 是否存在 `dist/index.html`。
- 是否存在 `dist/assets`。
- 构建过程是否有报错。

### 预期结果

前端构建成功，`frontend/dist` 已经是本次要发布的静态资源。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 3 步：本地执行后端测试

### 目标

确认后端接口、服务逻辑、数据模型相关测试可以通过。

### 为什么要做

这次后端也改了，并且数据库结构可能变化。后端测试能提前暴露接口返回、模型校验、权限逻辑、迁移逻辑里的问题。

### 你现在要做什么

另开一个 PowerShell，或者回到项目根目录后执行：

```powershell
cd backend
npm run test
```

如果测试很多，也可以先跑和本次变更直接相关的测试文件，例如：

```powershell
npm test public-reader.test.js knowledge-menu.test.js
```

### 执行后怎么检查

重点看：

- 是否全部通过。
- 是否有和 MongoDB、Mongoose、权限、文章、分类、用户相关的失败。
- 是否有测试超时或连接失败。

### 预期结果

后端测试通过，可以进入打包阶段。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 4 步：本地执行编码检查

### 目标

确认源码和文档没有 UTF-8 BOM，避免后端 JavaScript、配置文件、Markdown 出现编码问题。

### 为什么要做

项目要求所有源码和配置文件使用 UTF-8 无 BOM。虽然当前主要是 Node.js 和 Vue 项目，但编码混乱仍然可能导致中文乱码、脚本异常或后续维护困难。

### 你现在要做什么

回到项目根目录执行：

```powershell
cd ..
pwsh -File scripts\check-encoding.ps1
```

如果你当前已经在项目根目录，直接执行：

```powershell
pwsh -File scripts\check-encoding.ps1
```

### 执行后怎么检查

重点看是否输出：

```text
编码检查通过：未发现 UTF-8 BOM。
```

### 预期结果

编码检查通过。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 5 步：本地压缩前端发布包

### 目标

把 `frontend/dist` 压缩成可以上传到服务器的前端发布包。

### 为什么要做

服务器只需要前端构建后的静态文件。压缩后上传更稳定，也方便在服务器备份目录中保留发布包。

### 你现在要做什么

推荐从项目根目录执行统一打包脚本：

```powershell
pwsh -File scripts\package-release.ps1
```

这个脚本会同时生成：

```text
release/frontend-dist.zip
release/backend-release.zip
```

如果只想手工单独压缩前端，也可以在本机 `frontend` 目录执行：

```powershell
Compress-Archive -Path .\dist\* -DestinationPath .\frontend-dist.zip -Force
```

如果你不在 `frontend` 目录，也可以从项目根目录执行：

```powershell
Compress-Archive -Path .\frontend\dist\* -DestinationPath .\frontend\frontend-dist.zip -Force
```

### 执行后怎么检查

```powershell
dir .\frontend-dist.zip
```

或从项目根目录检查：

```powershell
dir .\frontend\frontend-dist.zip
```

如果使用统一打包脚本，则检查：

```powershell
dir .\release\frontend-dist.zip
```

重点看：

- 是否生成 `frontend-dist.zip`。
- 文件大小是否大于 0。
- 压缩包第一层应直接包含 `index.html` 和 `assets`，不能多套一层 `dist`。

### 预期结果

前端发布包生成成功。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 6 步：本地压缩干净的后端发布包

### 目标

生成后端发布包，但不包含 `.env`、`node_modules`、`uploads`。

### 为什么要做

这一步非常重要：

- `.env` 是服务器自己的生产环境配置，本地文件不能覆盖它。
- `node_modules` 应该在服务器 Linux 环境重新安装，本地 Windows 依赖不能直接带到服务器。
- `uploads` 是线上用户上传资源，不能被本地目录覆盖。

### 你现在要做什么

推荐使用统一打包脚本。正常情况下，第 5 步已经执行过：

```powershell
pwsh -File scripts\package-release.ps1
```

如果需要手工单独打包后端，再在本机项目根目录执行：

```powershell
$release = "backend-release"
Remove-Item -Recurse -Force $release -ErrorAction SilentlyContinue
New-Item -ItemType Directory $release | Out-Null

Copy-Item backend\package.json $release\
Copy-Item backend\package-lock.json $release\
Copy-Item backend\ecosystem.config.cjs $release\
Copy-Item backend\.env.example $release\ -ErrorAction SilentlyContinue
Copy-Item backend\src $release\src -Recurse
Copy-Item backend\tests $release\tests -Recurse -ErrorAction SilentlyContinue

Compress-Archive -Path ".\$release\*" -DestinationPath .\backend-release.zip -Force
Remove-Item -Recurse -Force $release
```

### 执行后怎么检查

```powershell
dir .\backend-release.zip
```

如果使用统一打包脚本，则检查：

```powershell
dir .\release\backend-release.zip
```

如果想进一步检查压缩包内容：

```powershell
Expand-Archive .\backend-release.zip .\backend-release-check -Force
dir .\backend-release-check
dir .\backend-release-check -Force
Remove-Item -Recurse -Force .\backend-release-check
```

重点确认不应该出现：

```text
.env
node_modules
uploads
```

### 预期结果

生成干净的 `backend-release.zip`，里面只包含后端运行所需源码、依赖清单、PM2 配置和可选测试文件。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 7 步：上传发布包到服务器

### 目标

把本地生成的前端和后端发布包上传到服务器备份目录。

### 为什么要做

服务器统一从 `/www/personal-blog/backups` 取发布包，便于复盘、回滚和排查是哪一次发布的文件。

### 你现在要做什么

使用 Xftp 或你习惯的上传方式，把下面两个文件上传到服务器：

```text
C:\Users\HN246\Desktop\全栈\个人全栈博客系统\release\frontend-dist.zip
C:\Users\HN246\Desktop\全栈\个人全栈博客系统\release\backend-release.zip
```

上传到：

```text
/www/personal-blog/backups/
```

### 执行后怎么检查

在服务器执行：

```bash
ls -lh /www/personal-blog/backups
```

重点看：

- 是否存在 `frontend-dist.zip`。
- 是否存在 `backend-release.zip`。
- 两个文件大小是否大于 0。

### 预期结果

两个发布包都已经上传到服务器。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 8 步：服务器创建本次发布备份目录

### 目标

为本次发布创建一个独立备份目录，后续数据库、前端、后端、上传文件都备份到这里。

### 为什么要做

发布不是只要能上线，还要能回退。按时间创建备份目录，可以清楚知道哪次发布对应哪些备份。

### 你现在要做什么

登录服务器后执行：

```bash
RELEASE_DIR=/www/personal-blog/backups/release-$(date +%Y%m%d-%H%M%S)
mkdir -p "$RELEASE_DIR"
echo "$RELEASE_DIR"
```

### 执行后怎么检查

```bash
ls -ld "$RELEASE_DIR"
```

### 预期结果

服务器输出本次发布备份目录，例如：

```text
/www/personal-blog/backups/release-20260619-153000
```

后续命令都使用同一个 `$RELEASE_DIR`。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 9 步：服务器备份当前 MongoDB 数据库

### 目标

发布前备份线上数据库。

### 为什么要做

这次数据库结构可能发生变化。无论最后是否执行迁移脚本，发布前都必须先备份线上库，避免迁移或新代码写入导致旧数据不可恢复。

### 你现在要做什么

在服务器执行：

```bash
mongodump --uri="mongodb://127.0.0.1:27017/personal_fullstack_blog" --out="$RELEASE_DIR/mongodb-before"
```

### 执行后怎么检查

```bash
find "$RELEASE_DIR/mongodb-before" -maxdepth 2 -type d
```

重点看是否存在：

```text
$RELEASE_DIR/mongodb-before/personal_fullstack_blog
```

### 预期结果

线上数据库备份完成。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 10 步：服务器备份当前前端、后端和上传文件

### 目标

备份当前线上代码和上传文件。

### 为什么要做

- 前端备份用于页面回滚。
- 后端备份用于接口回滚。
- `uploads` 备份用于防止误删线上媒体资源。

### 你现在要做什么

在服务器执行：

```bash
cp -a /www/personal-blog/frontend "$RELEASE_DIR/frontend-before"
cp -a /www/personal-blog/backend "$RELEASE_DIR/backend-before"
cp -a /www/personal-blog/uploads "$RELEASE_DIR/uploads-before"
```

### 执行后怎么检查

```bash
find "$RELEASE_DIR" -maxdepth 2 -type d
```

重点看是否存在：

```text
frontend-before
backend-before
uploads-before
```

### 预期结果

当前线上文件已经备份完成。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 11 步：服务器发布前端

### 目标

把新的前端静态资源发布到 Nginx 托管目录。

### 为什么要做

前端页面、样式、路由和接口调用的改动，只有替换 `/www/personal-blog/frontend` 后才会在线上生效。

### 你现在要做什么

在服务器执行：

```bash
rm -rf /www/personal-blog/frontend/*
unzip -o /www/personal-blog/backups/frontend-dist.zip -d /www/personal-blog/frontend
```

### 执行后怎么检查

```bash
ls -la /www/personal-blog/frontend
```

重点看：

- `index.html` 是否存在。
- `assets` 是否存在。

### 预期结果

前端新版本静态资源已经发布。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 12 步：服务器保存后端生产环境配置

### 目标

在替换后端代码前，单独保存服务器上的 `.env`。

### 为什么要做

`.env` 里有生产环境端口、数据库连接、JWT 密钥、管理员信息等配置。这个文件应该由服务器维护，不能被本地发布包覆盖。

### 你现在要做什么

在服务器执行：

```bash
cp /www/personal-blog/backend/.env "$RELEASE_DIR/backend.env.before-release"
```

### 执行后怎么检查

```bash
ls -lh "$RELEASE_DIR/backend.env.before-release"
```

### 预期结果

生产环境 `.env` 已经单独备份。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 13 步：服务器发布后端代码

### 目标

把新的后端代码发布到 `/www/personal-blog/backend`。

### 为什么要做

后端接口、数据模型、权限逻辑、上传逻辑等改动，需要替换后端代码并重启 PM2 后才会生效。

### 你现在要做什么

在服务器执行：

```bash
mv /www/personal-blog/backend /www/personal-blog/backend_old_$(date +%Y%m%d_%H%M%S)
mkdir -p /www/personal-blog/backend
unzip -o /www/personal-blog/backups/backend-release.zip -d /www/personal-blog/backend
cp "$RELEASE_DIR/backend.env.before-release" /www/personal-blog/backend/.env
```

### 执行后怎么检查

```bash
ls -la /www/personal-blog/backend
ls -la /www/personal-blog/backend/.env
```

重点看：

- `package.json` 是否存在。
- `package-lock.json` 是否存在。
- `src` 是否存在。
- `.env` 是否已恢复。
- 不应出现 `node_modules`。
- 不应出现本地打包带来的 `uploads`。

### 预期结果

后端新代码已经发布，生产环境 `.env` 已恢复。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 14 步：服务器安装后端依赖

### 目标

在服务器 Linux 环境重新安装后端依赖。

### 为什么要做

Node.js 依赖应该基于服务器系统安装，不能直接使用本地 Windows 的 `node_modules`。这样可以避免平台差异、二进制依赖、路径差异导致的问题。

### 你现在要做什么

在服务器执行：

```bash
cd /www/personal-blog/backend
npm install
```

### 执行后怎么检查

```bash
ls -la /www/personal-blog/backend/node_modules
```

### 预期结果

后端依赖安装完成。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 15 步：判断并执行数据库迁移

### 目标

根据本次数据库结构变化，决定是否需要执行一次性迁移脚本。

### 为什么要做

MongoDB 不强制表结构，但应用代码会依赖字段、类型、枚举和关系。如果旧数据不满足新代码预期，就需要迁移；如果新代码兼容旧数据，就不需要迁移。

### 判断方式

不需要迁移的常见情况：

- 只是新增非必填字段。
- 新字段有默认值。
- 页面或接口能处理字段为空。
- 旧数据不补字段也能正常展示。

需要迁移的常见情况：

- 字段改名。
- 字段类型变化。
- 旧枚举值不再被新代码识别。
- 新增必填字段，旧数据没有。
- 权限、角色、菜单结构变化。
- 文章、分类、媒体之间的关联方式变化。

### 你现在要做什么

如果不需要迁移，在这里记录：

```text
本次不需要数据库迁移，原因：待填写。
```

如果需要迁移，先确认迁移脚本文件，例如：

```text
backend/src/scripts/fixSomething.js
```

然后在服务器执行：

```bash
cd /www/personal-blog/backend
node src/scripts/fixSomething.js
```

如果迁移脚本已经配置到 `package.json`，也可以执行对应 npm 命令：

```bash
npm run 脚本名
```

### 执行后怎么检查

根据迁移内容写检查命令。示例：

```bash
mongosh mongodb://127.0.0.1:27017/personal_fullstack_blog --eval "print('articles=' + db.articles.countDocuments()); print('users=' + db.users.countDocuments());"
```

如果迁移权限菜单，可以检查：

```bash
mongosh mongodb://127.0.0.1:27017/personal_fullstack_blog --eval "print('roles=' + db.roles.countDocuments()); print('menus=' + db.menus.countDocuments());"
```

### 预期结果

数据库中的旧数据已经满足新后端代码需要。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 16 步：重启 PM2 后端服务

### 目标

让 PM2 重新加载新的后端代码和依赖。

### 为什么要做

服务器上的 Node.js 进程不会因为文件替换自动重启。必须重启 PM2 进程，新代码才会生效。

### 你现在要做什么

如果 `.env` 没变：

```bash
pm2 restart personal-blog-api
```

如果 `.env` 改过：

```bash
pm2 restart personal-blog-api --update-env
```

### 执行后怎么检查

```bash
pm2 list
pm2 logs personal-blog-api --lines 80
```

重点看：

- `personal-blog-api` 是否为 `online`。
- 日志里是否有启动错误。
- 是否有数据库连接失败。
- 是否有模块找不到、环境变量缺失等错误。

### 预期结果

后端服务在线，新代码已经生效。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 17 步：服务器接口健康检查

### 目标

先在服务器本机确认后端接口和 Nginx 代理都正常。

### 为什么要做

如果服务器本机都访问不了，浏览器公网访问也一定有问题。先用 `curl` 可以快速定位问题是在后端、Nginx 还是公网访问层。

### 你现在要做什么

在服务器执行：

```bash
curl http://127.0.0.1:3001/api/health
curl http://127.0.0.1/api/health
```

### 执行后怎么检查

重点看返回中是否包含：

```json
{"success":true}
```

第一条检查后端直连，第二条检查 Nginx 反向代理。

### 预期结果

后端和 Nginx 代理都正常。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 18 步：浏览器验证核心页面

### 目标

从用户视角确认前端、后端、登录态、权限、数据读取都正常。

### 为什么要做

`/api/health` 只能说明服务启动了，不能证明业务页面可用。发布后必须人工打开核心页面检查。

### 你现在要做什么

在浏览器访问：

```text
http://服务器IP/
http://服务器IP/login
http://服务器IP/register
http://服务器IP/console/articles
http://服务器IP/console/search
http://服务器IP/console/profile
http://服务器IP/console/manage/articles
http://服务器IP/console/manage/comments
http://服务器IP/console/manage/users
http://服务器IP/console/manage/media
http://服务器IP/console/manage/notifications
http://服务器IP/console/manage/settings
```

如果域名已经备案并解析正常，也可以把 `服务器IP` 替换成域名。

### 执行后怎么检查

重点看：

- 首页是否能打开。
- 登录是否成功。
- 普通用户和管理员菜单是否符合权限。
- 知识库左侧目录是否正常。
- 文章列表、详情是否正常。
- 后台文章、评论、用户、媒体、公告、设置是否正常。
- 上传图片或历史图片是否能通过 `/uploads` 显示。
- 控制台浏览器 Network 是否有 4xx、5xx。

### 预期结果

核心页面可用，本次发布成功。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 19 步：发布后日志复查

### 目标

确认用户实际访问核心页面后，后端日志没有新的严重错误。

### 为什么要做

`/api/health` 只能证明服务启动成功，浏览器页面能打开也不代表所有接口都完全正常。日志可以发现接口参数错误、权限错误、旧数据字段不兼容、上传资源缺失等问题。

### 你现在要做什么

在服务器执行：

```bash
pm2 logs personal-blog-api --lines 120
```

该命令会持续监听日志，看完最近 120 行后按：

```text
Ctrl + C
```

退出日志查看。退出日志不会停止后端服务。

### 执行后怎么检查

重点判断：

- `GET/POST ... 200`：接口正常。
- `304`：浏览器缓存命中，通常正常。
- `401 /api/auth/me`：未登录时探测登录态，通常正常。
- `404 /uploads/...`：多半是历史上传文件缺失，记录下来后续单独处理。
- `400`：检查前端请求参数是否超过后端校验范围。
- `500`、`ValidationError`、`CastError`、`Unhandled`、数据库连接错误：阻塞发布，需要优先处理。

### 常见处理记录

如果看到：

```text
GET /api/rbac/roles?page=1&pageSize=200 400
```

说明前端全量拉取角色时 `pageSize` 超过后端允许值。优先把前端 `pageSize` 调整到后端允许范围，例如 `100`，重新构建并只发布前端。

如果看到：

```text
Cannot read properties of undefined (reading 'importKey')
```

这是浏览器登录页报错。常见原因是通过 `http://服务器IP` 访问时浏览器不提供 `window.crypto.subtle`。短期处理是发布支持 HTTP 试用环境的前端兼容逻辑；长期处理是配置 HTTPS。

### 预期结果

没有新的 500 或阻塞级错误。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 20 步：仅前端热修复发布

### 目标

如果发布后只发现前端问题，快速重新构建并覆盖前端静态文件，不动后端和数据库。

### 为什么要做

前端热修复只影响 `frontend/dist`。如果后端代码没有变化，不需要重新上传后端包、不需要执行 `npm install`、也不需要重启 PM2。

### 适用场景

- 登录页 JS 报错。
- 页面样式问题。
- 前端请求参数错误。
- 路由、菜单、表格展示问题。

### 你现在要做什么

本地执行：

```powershell
cd frontend
npm run build
cd ..
pwsh -File scripts\check-encoding.ps1
pwsh -File scripts\package-release.ps1
```

上传本地文件：

```text
C:\Users\HN246\Desktop\全栈\个人全栈博客系统\release\frontend-dist.zip
```

覆盖服务器文件：

```text
/www/personal-blog/backups/frontend-dist.zip
```

服务器执行：

```bash
ls -l /www/personal-blog/backups/frontend-dist.zip
rm -rf /www/personal-blog/frontend/*
unzip -o /www/personal-blog/backups/frontend-dist.zip -d /www/personal-blog/frontend
ls -la /www/personal-blog/frontend
```

### 执行后怎么检查

浏览器打开问题页面并强制刷新：

```text
Ctrl + F5
```

如果仍然加载旧 JS，打开开发者工具，在 Network 面板勾选 `Disable cache` 后刷新。

### 预期结果

前端问题修复生效，后端服务状态不变。

### 实际结果

```text
待填写
```

### 遇到的问题

```text
待填写
```

### 最终怎么解决

```text
待填写
```

## 第 21 步：发布失败时回滚前端

### 目标

如果新前端页面不可用，恢复发布前的前端静态资源。

### 为什么要做

前端回滚不会影响数据库，通常可以快速恢复页面访问。

### 你现在要做什么

在服务器执行：

```bash
rm -rf /www/personal-blog/frontend/*
cp -a "$RELEASE_DIR/frontend-before/." /www/personal-blog/frontend/
```

### 执行后怎么检查

```bash
ls -la /www/personal-blog/frontend
```

然后浏览器刷新首页。

### 实际结果

```text
待填写
```

## 第 22 步：发布失败时回滚后端

### 目标

如果新后端启动失败或核心接口不可用，恢复发布前的后端代码。

### 为什么要做

后端问题会影响登录、文章、后台管理等核心功能，需要快速恢复到上一个可用版本。

### 你现在要做什么

在服务器执行：

```bash
pm2 stop personal-blog-api

rm -rf /www/personal-blog/backend
cp -a "$RELEASE_DIR/backend-before" /www/personal-blog/backend

cd /www/personal-blog/backend
npm install
pm2 restart personal-blog-api --update-env
```

### 执行后怎么检查

```bash
pm2 list
curl http://127.0.0.1/api/health
```

### 实际结果

```text
待填写
```

## 第 23 步：发布失败时回滚数据库

### 目标

如果数据库迁移导致数据不可用，并且确认需要恢复发布前数据，则执行数据库回滚。

### 为什么要做

数据库回滚会覆盖发布后产生的数据，所以必须比前端、后端回滚更谨慎。只有确认新版本写入的数据可以放弃时，才执行这一步。

### 你现在要做什么

确认要回滚数据库后，在服务器执行：

```bash
mongorestore --uri="mongodb://127.0.0.1:27017" --drop "$RELEASE_DIR/mongodb-before"
pm2 restart personal-blog-api
```

### 执行后怎么检查

```bash
curl http://127.0.0.1/api/health
```

并重新打开核心页面验证。

### 实际结果

```text
待填写
```

## 本次发布结论

### 是否发布成功

- [ ] 成功
- [ ] 部分成功
- [ ] 已回滚

### 最终线上版本状态

```text
待填写
```

### 后续待处理事项

```text
待填写
```

### 经验记录

```text
待填写
```
